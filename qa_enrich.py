# -*- coding: utf-8 -*-
"""为六篇论文笔记追加「自问自答」区（可折叠问答）。格式：每条为 "问题|||答案"。"""
import pathlib

PAPERS = pathlib.Path(r"C:\Users\ChengyeLin.LINOUP-XIAOXIN1\Desktop\work\paper-notes\docs\papers\2026-09")

QA = {
    "0918-biot.md": [
        "为什么逐通道独立切分，而不是所有通道拼在一起切？|||全通道拼接切分隐含假设通道数固定——换个数据集通道数变了就无法对齐。逐通道切分后每个 token 天然带通道 embedding，缺哪个通道就把对应 token 丢掉，模型结构完全不用改，这是 BIOT 能吃下异构格式的根本原因。",
        "FFT 能量特征丢掉了相位，有什么后果？|||频谱能量只描述有哪些频率、各多强，不描述相位对齐关系。对癫痫检测、情绪等频域主导的任务影响小；但 ERP 类任务（P300 在刺激后约 300ms 的相位锁定偏转）依赖精确的时域波形，EEGPT 报告 BIOT 在 PhysioP300 上仅 0.5485，并把它归因于此。",
        "线性注意力（Linformer 式）牺牲了什么？|||E/F 投影把 N×N 注意力压成 N×d，隐含假设全局信息可以压缩进 d 个摘要位。代价是丢掉了细粒度的 token 间路由——每个 token 只能看 d 个全局摘要而非每个具体 token。对句子级分类（最后 mean pooling）足够，但不适合需要精确定位关系的任务。",
        "为什么用 mean pooling 而不是 [CLS] token？|||作者实验发现加 [CLS] 的性能略差。可能原因：BIOT 的句子 token 顺序是按通道依次拼接而非自然语序，[CLS] 汇聚对位置敏感；mean pooling 对所有 token 等权平均，更稳健。",
    ],
    "0918-labram.md": [
        "为什么重构傅里叶频谱而不是原始波形？|||EEG 低信噪比、强随机、非平稳，作者前期实验里直接重构原始波形的 loss 根本不收敛。频谱的幅值/相位分布反映底层神经生理活动且更稳定可学——换个重构域是这篇最关键的实验发现，NeuroLM（去掉相位）和 TFM（掩码频谱图）都是这条线的延续。",
        "codebook 是不是可有可无？直接掩码重构不行吗？|||附录 Table 7 的消融：不用 codebook、直接重构原始信号或频谱（Setting 2/3），在低层任务 TUAB 上反而略好，但在高层任务 TUEV 上明显下降（0.6409 → 0.5730 / 0.5643）。离散语义码学到的抽象表示对区分事件类型这类高层任务至关重要。",
        "对称掩码为什么能省计算？|||tokenize 一次的开销很大（要过整个 encoder）。对称掩码对同一样本生成 M 和它的补集两个视角，共享同一次 tokenizer 前向的离散结果，相当于白拿一次数据增强；附录消融显示参数越大的模型受益越明显。",
        "为什么 linear probing 在 TUEV 上崩了（0.346）？|||说明 LaBraM 的表示与全量微调强绑定：掩码码字预测学到的特征停留在预测码字够用的层次，不经微调无法线性映射到任务空间。对比 EEGPT 把 linear probing 做成核心卖点，暴露了两种预训练目标在表示质量上的差异。",
    ],
    "0918-eegpt.md": [
        "为什么 encoder 处理的是被 mask 的部分，和 MAE 正好相反？|||这是显式表示 z 设计的一部分：encoder 只看 50% 时间 × 80% 通道的稀疏视图，却要靠 predictor 预测全量特征、与看过完整信号的 momentum encoder 对齐——逼着稀疏视图的输出携带全局语义。若 encoder 直接看全量，对齐任务就没有信息瓶颈，学不到这个性质。",
        "去掉 predictor 行不行？|||附录 Figure 6：去掉 predictor 后对齐目标变成 encoder 与 momentum encoder 直接对齐，两者可以走捷径坍缩到相同输出（表示坍塌），重构 loss 也不再下降。predictor + 可学习 query 提供了必要的不对称性，与 BYOL 需要 predictor 同理。",
        "为什么坚持 linear probing 而不微调？|||两个动机：一是 EEG 下游标注少，全量微调大模型极易过拟合；二是 linear probing 让性能完全归因于 encoder 的表示质量——它是更严格的通用表示评估协议，EEGPT 在这个协议下超过全量微调的 BENDR，说服力更强。",
        "Codex book 怎么处理训练时没见过的通道？|||它维护通道名到可学习向量的映射表，覆盖常见命名体系，新数据集的通道按名字映射即可。但表是固定的（58 电极），碰到 ear-EEG 这类体系外通道就没有对应向量——这是它跨设备能力弱于 TFM 单通道方案的原因。",
    ],
    "0918-braingpt.md": [
        "为什么自回归比掩码建模更适合 EEG？|||论文的消融（同架构、同损失度量）显示 AR 全面优于 MAE 平均 2% 以上：EEG 反映连续渐进的信息流，过去活动影响未来状态，因果的 next-token 预测天然贴合；MAE 的双向补全破坏了这种时序因果结构。且 AR 预测的是真实未来而非被人为挖掉的洞，任务更难、学到的表示更强。",
        "TEG 图里到底存了什么多任务信息？|||图结构本身不存任务信息。多任务信息来自训练方式：节点向量 V_m（电极原型）和 GAT 权重是所有任务的梯度共同更新的共享参数——同一电极被多个任务使用时，它的 V_m 同时接收多个任务的监督信号。推理时输入的电极集决定激活哪个子图（β mask），任务头决定读出。若两个任务电极集完全不相交，空间层面的共享就只剩 GAT 权重。",
        "电极级拆样本有什么代价？|||样本量乘以电极数倍增（3750 万）有水分：单电极序列丢掉了跨电极的同步信息（空间关系、参考电极的共模成分），预训练学到的只是单电极时序动力学，空间整合完全押注在下游 TEG 上——这就是作者所说的渐进式时空解耦。",
        "末尾追加的 special token c 为什么能聚合成全局表示？|||ETE 是因果（单向）注意力：序列末尾的 token 天然能注意到它之前的所有 token。把 c 放在每个电极序列的最后，它的输出位置就自动汇聚了该电极整段时序的信息，类似 GPT 里用最后一个 token 做分类。",
    ],
    "0918-neurolm.md": [
        "为什么选择空间对齐而不是 CLIP 式的 embedding 对齐？|||CLIP 式对齐需要大规模成对数据（图文对），而 EEG-text 对几乎不存在——一段 EEG 同时包含情绪、运动、病理等多重信息，人类语言无法完整标注。所以 NeuroLM 退而求其次做 space-wise 对齐：只要求 EEG embedding 落在与文本相同的分布空间里（域分类器 + 梯度反转），不要求逐条语义对应。",
        "GRL（梯度反转层）是怎么工作的？|||前向传播时是恒等变换，反向传播时把梯度取反。效果：域分类器 C 正常训练（学会分辨 EEG 和文本），而 VQ encoder 收到反向梯度、朝骗过分类器的方向更新——两者的极小极大博弈把 EEG embedding 推进文本 embedding 空间。λ 从 0 渐增到 1，避免训练初期扰动过大。",
        "为什么不能像语言那样逐 token 自回归？|||语言是单序列、词序固定；EEG 的通道数和排列因数据集而异，没有统一的下一个 token。阶梯掩码的答案：同通道 token 预测同通道下一时间步（时序因果），同时每个 token 可见所有通道的当前与历史（空间全可见）——把语言的 AR 结构迁移到通道乘时间的二维格子上。",
        "为什么每批要混入少量纯文本数据？|||防止 LLM 的语言能力灾难性遗忘。指令微调全部是 EEG 到短答案的任务，若纯用这些数据训练，GPT-2 的通用语言分布会迅速退化，泛化到新 prompt 的能力也随之丧失。",
    ],
    "0918-tfm-tokenizer.md": [
        "为什么 tokenizer 内要去掉位置编码？|||消融证据：去掉 PE 后 TUEV Kappa 从 0.5119 升到 0.5337，token 利用率下降（12.87% 到 9.78%）、类独有 token 上升。机理：EEG motif 非平稳，同一模式可出现在任意位置；加了 PE，相同 motif 会因位置不同被学成不同 token，造成词表冗余。motif 词表需要的是平移不变性。注意：下游 Transformer 仍有位置编码，去掉的只是 tokenizer 内部的。",
        "频域路径为什么要把频谱窗沿频率轴切 patch？|||BIOT 的做法是把整个频谱窗直接过一个线性层，频率信息被一次性压缩，模型容易只看能量最大的低频段。沿频率轴切 patch 后用频率 Transformer 建模窗内跨频段依赖，再用 sigmoid 门控聚合——让哪些频段重要变成可学习的决策（睡眠任务可以学会忽略 32Hz 以上）。",
        "为什么频带掩码比随机掩码重要得多？|||消融：随机掩码 Kappa 0.4772，频带掩码 0.5193（+8%）。随机掩小块太容易靠邻域插值恢复；按频带整条掩掉迫使模型从其他频带和时域上下文推断缺失频段的结构，学到的是跨频段依赖而非局部平滑。",
        "这套 tokenization 的主要局限是什么？|||作者自认：固定窗长可能把跨越窗口的大 pattern 切断，同一事件被分配到不同 token（误判为不同 motif）。此外 VQ 查表是硬量化，边界附近的微小波形差异可能被吞掉；ear-EEG 实验也表明跨设备泛化仍依赖微调数据量。",
    ],
}


def main() -> None:
    for fname, qas in QA.items():
        path = PAPERS / fname
        text = path.read_text(encoding="utf-8")
        if "## 自问自答" in text:
            print("skip:", fname)
            continue
        lines = ["## 自问自答\n"]
        for i, item in enumerate(qas, 1):
            q, a = item.split("|||", 1)
            lines.append(f'??? question "Q{i} · {q}"\n')
            lines.append(f"    {a}\n\n")
        section = "\n".join(lines).rstrip() + "\n"
        text = text.rstrip() + "\n\n" + section
        path.write_text(text, encoding="utf-8")
        print(f"[+] {fname}: {len(qas)} 条问答")


if __name__ == "__main__":
    main()
