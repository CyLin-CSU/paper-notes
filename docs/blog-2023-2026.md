---
title: 综述 · EEG 基础模型三年演进（2023–2026）
tags: [综述, EEG, 基础模型]
---

# 综述：EEG 基础模型三年演进（2023–2026）

> 基于 10 篇代表性论文（核心六篇：BIOT / LaBraM / EEGPT / BrainGPT / NeuroLM / TFM-Tokenizer；2025–2026 扩展：LUNA / EEGMoE / JET / RF-GPT）的精读综述。
> 完整方法拆解见站内[论文笔记](papers/2026-09/0918-biot.md)，本文是对外的观点性总结。

## TL;DR

三年间，EEG 基础模型在三个维度上各走完了一条演进线：**输入表示**从规则切段走到可学习的时频 motif 词表，**预训练目标**从对比学习/掩码建模扩展到自回归，**使用范式**从"一任务一模型"走向接 LLM 的多任务指令推理。但所有模型的通用性仍被同一个东西卡住：**数据**——2.5 万小时已是极限规模，且被临床癫痫域重度偏斜。2025–2026 的最新一批工作开始正面回应这个天花板：**LUNA/EEGMoE** 分别从"统一拓扑"与"解耦域"两端管理数据异构性，**JET** 把生成建模（流匹配）做成缓解数据稀缺的正式方案，**RF-GPT** 则在射频域证明"时频图 + 大模型"范式跨模态通用。

## 一、输入表示：token 的三次升维

BIOT（NeurIPS'23）确立了"逐通道切段"的统一格式：通道可缺、长度可变、缺失即丢 token，配合线性注意力处理长句子。它的 token 是连续的、无词表的——"tokenize 了，但没有词汇表"。

LaBraM（ICLR'24）补上了词表：VQ-VAE 式码本（8192）+ 傅里叶频谱重构目标。关键实验发现是**重构域的选择**：EEG 低信噪比导致原始波形不可重构，频谱可以。但它的码字只当预训练的监督信号，推理时被丢弃。

NeuroLM（ICLR'25）让码字真正上岗：并入 GPT-2 词表，EEG 变成"外语句子"。TFM-Tokenizer（ICLR'26）则给出终审意见——LaBraM 的 token 化不彻底；它学的是**时频 motif** 词表（频域路径显式建模跨频段依赖），并把 token 作为下游模型的真正输入，以 1.9M 参数拿下多项 SOTA。

**演进的逻辑主线**：预处理产物 → 训练靶子 → 真实输入。

## 二、预训练目标：掩码建模的内部革命与自回归的挑战

掩码建模内部经历了三次目标更替：原始波形（不收敛，失败）→ 频谱（LaBraM）→ 表示（EEGPT 的 JEPA 式对齐 + MAE 式重构双自监督）。EEGPT 的贡献是指出"掩码后恢复什么"比"是否掩码"更重要——在表示空间对齐比在信号空间重构学到的东西更通用，这让它用 linear probing 就达到 SOTA。

BrainGPT（2024）则质疑范式本身：同架构同损失的消融显示，**自回归比掩码建模平均高 2%+**——EEG 是连续因果的信息流，双向补全破坏了它的自然结构。它把模型推到 1.09B 参数并验证了 scaling law，代价是需要下游图网络把单电极预训练丢掉的空间信息补回来。

## 三、使用范式：从 specialist 到 generalist 的两条路

BrainGPT 用**任务共享图网络**联合训练 12 个基准——多任务信息的载体不是图结构，而是"电极节点被多任务梯度共同更新"；小任务受益最大（+3.9%），本质是隐式数据增强。

NeuroLM 走了更激进的路：把 EEG 当外语接进 GPT-2，指令微调统一六种任务。它诚实报告了性能仍逊于单任务 SOTA，且论文称有望泛化到新 prompt（潜力表述，原文未见泛化实验验证）。**多任务的价值目前在范式与效率，不在数字**。

## 四、绕不开的天花板：数据

- LaBraM：Base 500h 在 TUAB 反超 2500h、TUEV 达其 90%+，Huge 在 2500h 仍未饱和；
- NeuroLM：L 与 XL 验证困惑度接近 → 25000h 喂不饱十亿参数；
- BrainGPT：数据 0→1B token 性能持续上升但增益趋缓。

规模瓶颈之外是**域偏斜**：能拿到万小时级的只有临床 TUEG 体系，这解释了为什么 pretrain-then-finetune 在脑机接口类任务上的收益普遍不如临床任务——BrainGPT 甚至观察到预训练 specialist 弱于从头训练。

## 五、2025–2026：问题重心转移的三条新轴线

上面的故事止于"怎么表示 EEG"；2025–2026 的新工作把问题推到了三个新方向。

**轴线一：异构管理——统一还是解耦？** EEG 数据的"不齐"有两个轴。电极拓扑轴上，**LUNA**（NeurIPS 2025）用学习 query + 交叉注意力把任意电极布局压进固定隐空间，时间注意力只在隐空间上进行——通道复杂度从平方降到线性，8000 通道时 FLOPs 仅为 BIOT 的 1/180，TUAR/TUSL 双 SOTA；代价是隐空间瓶颈税（TUAB 落后全注意力约 1pp）。任务/数据集域轴上，**EEGMoE**（TNNLS 2026）实证了多任务联合预训练的梯度冲突，用 Specific+Shared 双专家 MoE 把域差异解耦保留——1.68M 参数（激活 0.89M）就在四个基准上全面超过复现的 BIOT/LaBraM，专家-任务分工可视化证明解耦真的发生了。两条路线哲学相反（拓扑该抹平、域该解耦），正好各管一个轴。

**轴线二：生成——数据瓶颈的正面进攻。** **JET**（ICML 2026）把 EEG 生成建模为连续动力过程：条件流匹配学一个把噪声输运到数据分布的向量场，配合三条从 EEG 统计结构推出的约束（L1 重构抗伪迹、矩一致防漂移、TV+Pearson 保结构）。TS-FID 比强基线降 40%+ 只是表象，更关键的是**下游增强收益为正而 GAN/扩散基线≈0**——生成数据第一次被证明"真的有用"。这直接回应了第四节的数据天花板：临床数据贵且受隐私限制，高保真合成是规模化的替代路径。

**轴线三：跨域镜像。** **RF-GPT**（arXiv 2026）在射频域完成了"时频图 + 大模型"的完整落地：谱图直接复用现成 VLM 视觉编码器、纯合成数据零人工标注 SFT。它给 EEG 研究者两个同样值钱的提醒：通用 VLM（含 GPT-5）读不懂 RF 谱图（**"长得像图"不等于"有先验"**）；通道失配（IQ 不平衡）是时频输入方案最致命的损伤，而这一点与 EEG 的电极阻抗失配完全同构。

## 六、给研究者的四点启示

1. **做 tokenization 的人该看 TFM**：词表要学、位置编码要去、单通道要独立——三个反直觉设计都有消融支撑；
2. **做预训练的人该看 EEGPT 与 BrainGPT 的分歧**：掩码 vs 自回归之争目前 AR 略胜，但双向建模在"整段理解"任务上的潜力未被证伪；
3. **做应用的人该看 NeuroLM 的 limitation 章节**：多任务统一模型的现实成本（超参敏感、粗粒度对齐）写得非常坦诚；
4. **做异构与生成的人该看 2025–2026 这批**：LUNA 证明拓扑可以抹平（1pp 的代价）、EEGMoE 证明域应该解耦（1.68M 就够）、JET 证明生成数据真能提升下游（而 GAN/扩散不能）——三者合起来正好对应"数据异构"与"数据稀缺"两大瓶颈的工程解。

## 延伸阅读

- 完整对比：[十篇方法对比](comparison.md)（核心六篇 14 维表 + 扩展四篇速览 + benchmark 战绩 + 论文互怼链）
- 主题深挖：[Tokenization 演进](topics/tokenization.md) · [预训练范式](topics/pretraining.md) · [通道异构](topics/spatial.md) · [数据全景](topics/data-landscape.md) · [多任务路线](topics/multi-task.md) · [基座来源](topics/base-model.md)
- 架构基础：[LoRA](basics/lora.md) · [Attention 替代方案](basics/attention-alternatives.md)
- 每篇精读：见左侧「论文笔记」
